# 実装計画

作成日時: 2026-06-12 22:44
更新日時: 2026-06-12 23:20

## アーキテクチャ

```
+--------------------------------------------------+
| Electron アプリ                                   |
|                                                  |
|  メインプロセス (Node.js)                          |
|   - 気象データのダウンロード (NOMADS / CDS)        |
|   - GRIB2 → JSON 変換 (将来: wgrib2 同梱)         |
|   - ローカルキャッシュ (userData ディレクトリ)      |
|   - IPC でレンダラーにデータ提供                    |
|                                                  |
|  レンダラープロセス (Three.js / WebGL)             |
|   - 地球儀 (SphereGeometry + テクスチャ)           |
|   - 風パーティクルの移流と流線描画                  |
|   - カメラ操作 (OrbitControls)                    |
|   - UI (日時選択、高度選択、表示設定)               |
+--------------------------------------------------+
```

- セキュリティ方針: `contextIsolation: true` / `nodeIntegration: false` とし、
  レンダラーからのデータ取得は preload 経由の IPC に限定する。
- ビルド: レンダラーは Vite でバンドル、メインプロセスと preload は esbuild でバンドルする。
  開発サーバーは使わず、`npm run build` → `npm start` のシンプルな構成から始める。

## 風データの形式

cambecc/earth (nullschool のオープンソース版) と同じ GFS JSON 形式を内部形式として採用する。

- U 成分(東西風)と V 成分(南北風)の 2 レコードからなる配列。
- 各レコードは `header` (nx, ny, lo1, la1, dx, dy など格子情報) と `data` (行優先の値配列) を持つ。
- 格子は緯度経度の等間隔格子(プロトタイプは 1.0°、将来は 0.25°)。
- 北端 (la1=90) から南へ、西端 (lo1=0) から東へ走査する。

この形式なら grib2json (nullschool 作者の変換ツール) の出力をそのまま読める。

## フェーズ計画

### P0: プロトタイプ(完了 2026-06-12)

- [x] 技術選定とアーキテクチャ決定
- [x] Electron + Vite + TypeScript + Three.js の骨組み
- [x] 3D 地球儀の表示と回転・ズーム
- [x] 風データ(GFS JSON 形式)の読み込み
  - 同梱のサンプルデータを第一とし、ネット取得は P1 で本実装する
  - データが無い場合は合成風場(帯状流)でフォールバック
- [x] CPU ベースのパーティクル移流 + 流線描画(数千〜1万粒子)
- [x] 風速に応じた色付け

### P1: 実データ取得

- [ ] NOMADS filter エンドポイントから GFS の UGRD/VGRD (10m) を取得
- [ ] GRIB2 → JSON 変換の組み込み(wgrib2 または grib2json の同梱、もしくは JS デコーダの調査)
- [ ] userData へのキャッシュと更新(GFS は 1 日 4 回更新)
- [ ] 日時選択 UI(予報時刻の切り替え)

### P2: 過去データ (ERA5)

- [ ] Copernicus CDS API での ERA5 取得(API キーの設定 UI)
- [ ] 過去日時の選択 UI(カレンダー / タイムスライダー)
- [ ] 取得済みデータの一覧・管理画面

### P3: 描画の高度化

- [ ] GPU パーティクル化(ping-pong FBO で数十万粒子)
- [ ] 風速のカラーオーバーレイ(地表の色塗り)
- [ ] 等圧面(850hPa / 500hPa / 250hPa など)の高度切り替え
- [ ] 0.25° 格子への対応

### P4: エディタ機能・配布

- [ ] 表示設定(粒子数、軌跡長、配色、速度倍率)の編集 UI
- [ ] スクリーンショット / 動画書き出し
- [ ] electron-builder によるパッケージング

## UI 方針

`docs/rules/electron-design-rules.md` に従う。
ダークテーマ基調(地球儀の見栄え優先)、コントロールは控えめな右パネルまたは下部バーに置く。

## リスクと対策

| リスク | 対策 |
|---|---|
| GRIB2 の JS パースが弱い | wgrib2 / grib2json を同梱しメインプロセスから呼ぶ。P0 ではサンプル JSON で回避 |
| CPU パーティクルの性能限界 | P0 は 1 万粒子以下に抑え、P3 で GPU 化 |
| CDS API の登録が必要 | P2 で API キー設定 UI を用意。P0/P1 は GFS のみで成立させる |
| 極付近の移流の歪み | 緯度による経度補正 (1/cosφ) と極近傍での粒子リセットで対処 |
